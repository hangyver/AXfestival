{
  "name": "kpa-academic-festival-2026",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:sites": "node scripts/clean-dist.mjs && vite build --outDir dist/client && node scripts/prepare-sites-build.mjs",
    "preview": "vite preview --host 0.0.0.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
